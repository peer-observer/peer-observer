[package]
name = "metrics"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../../shared" }

[dev-dependencies]
# For checking the Grafana dashboards in tests/dashboards.rs
serde_json = "1.0"

[features]
# Treat warnings as a build error.
strict = []
# Run integration tests needing a NATS server.
nats_integration_tests = []
